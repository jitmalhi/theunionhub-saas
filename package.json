{
  "name": "theunionhub-online",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-tenant SaaS platform for theunionhub.com on Vercel + Supabase.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "echo 'No build step needed'",
    "deploy": "vercel deploy --prod",
    "new-tenant": "node scripts/new-tenant.mjs"
  },
  "dependencies": {
    "@vercel/edge": "^1.3.1"
  },
  "devDependencies": {
    "vercel": "^41.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
